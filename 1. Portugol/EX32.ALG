// Algoritmo da tabuada
Algoritmo "EX08"

// Variáveis
Var
   i, n: inteiro  // Contador e número

Inicio
   // Solicita número
   escreva("# Deseja a tabuada para qual valor: ")
   leia(n)

   escreval("")

   // Tabuada de 1 a 10
   para i de 1 ate 10 passo 1 faca
      escreval("#", n, " x", i, " =", n * i)
   fimpara

Fimalgoritmo
